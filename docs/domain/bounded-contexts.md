<a id="bounded-contexts"></a>
# 바운디드 컨텍스트 지도 (P1~P4)

- 상태: 설계 초안 — P1은 모듈 골격 생성과 `schedule` 도메인 모델 구현, P2~P4는 코드 없음
- 기준일: 2026-09-18
- 기준 자료: 기획/기획안.md(오빗 서비스 기획서), [오빗 유즈케이스 분리안](../planning/use-cases.md)

이 문서는 DDD 관점에서 오빗의 바운디드 컨텍스트(BC)를 서브도메인 분류·소유 애그리게잇·컨텍스트 간 관계까지 제안한다. 현재 등록된 모듈 목록·책임 요약·공개 계약·허용 의존성의 원본은 [도메인 지도](README.md#모듈별-책임과-공개-계약)다. 적용된 범위는 `organization`·`schedule`·`notification` 모듈 골격 추가와 `schedule`의 도메인 모델([작업 상태·배정 정책](#schedule-policies) 포함)이며, 그 밖의 애그리게잇·관계와 P2~P4는 설계 초안이다. 골격 모듈의 허용 의존성은 현재 없고, 계획된 관계를 구현할 때 필요한 공개 계약과 의존성을 함께 추가한다.

## 범례

| 구분 | 의미 |
| --- | --- |
| **서브도메인** | Core(핵심 경쟁력) / Supporting(핵심을 돕는) / Generic(범용) |
| **상태** | `구현 중` — `domain`과 일부 Application·Adapter가 있으나 공개 계약·완성된 유즈케이스는 없음 / `도메인 구현` — `domain` 패키지만 존재, Application·Adapter·공개 계약 없음 / `골격 생성` — 모듈 루트의 `package-info.java`만 존재, 도메인 코드 없음 / `미착수` — 폴더도 없음 |
| **관계 표기** | `참조(ID)` — 동기 조회, 상대 공개 계약만 호출 / `이벤트` — 비동기, Published Language / `ACL` — 상대 모델을 자기 언어로 번역해 수용 |

---

## P1 — MVP 골격 생성 완료

### 1. 인증 컨텍스트 (`auth`) — Generic Subdomain

| 항목 | 내용 |
| --- | --- |
| 책임 | 로그인 수단(현재 예제 코드는 subject 조회) 처리. accountId 발급까지가 책임 범위 |
| 상태 | 기존 예제 모듈 재사용 — 실제 소셜/이메일 로그인 도메인 코드로 교체 예정 |
| 소유 개념 | `AuthSubject`(예제) → 실제로는 계정 자격증명·세션 발급을 다루는 애그리게잇으로 교체 |
| 관계 | `organization`이 이 모듈의 공개 계약을 참조(ID)해 accountId 유효성을 확인 (Conformist) |

> **"계정"이라는 이름을 조직 컨텍스트에서 뺀 이유**: "계정"은 마이페이지·내 정보 수정·회원탈퇴처럼 개인정보 관리 뉘앙스가 강해서, 조직 소속을 다루는 컨텍스트와 이름이 섞이면 안 된다고 판단했다. 개인 계정·프로필 관리 자체를 어느 컨텍스트가 가질지(이 `auth`인지, 별도 컨텍스트인지)는 아직 미정 — §미해결 이슈 참고.

<a id="organization"></a>
### 2. 조직 컨텍스트 (`organization`) — Supporting Subdomain

| 항목 | 내용 |
| --- | --- |
| 책임 | 조직(발주사) 설정, 유형(직원/기사/작업유형) 관리, 회사 참여 요청, 조직 소속(Membership) 관리 |
| 상태 | 골격 생성 (`package-info.java`만 존재) |
| 소유 애그리게잇 | **Organization**(Root) — 조직명·업종, 유형(직원/기사/작업유형) 보유<br>**MembershipRequest**(Root) — 사용자가 회사 코드·링크·QR로 생성, 희망 유형 보유, 대기→승인(유형 확정)/거절/취소(사용자)<br>**Membership**(Root) — role(Owner/Staff/Technician)·조직 소속·활성상태, authAccountId를 불투명 참조로만 보유. 사용자는 여러 조직에 소속될 수 있음(N:M), 같은 조직 내 중복 소속·대기 요청만 차단 |
| 관계 | `auth` ← 참조(ID) (accountId 존재 확인, ACL) · `schedule` → 참조(ID) 제공 (계정·조직으로 Membership 조회) · `notification` ← 이벤트 발행(`MembershipRequestApproved`) |

**참여 요청을 별도 모듈로 분리하지 않은 이유**: 회사 코드와 참여 요청은 소속 생성 규칙에 포함되고, 승인과 소속 생성이 하나의 유즈케이스에서 함께 성공·실패해야 하므로 `organization` 안의 애그리게잇으로 둔다(`docs/planning/use-cases.md` §3). 요청 생명주기가 복잡해지거나(다중 소속 검증 등) 별도 팀 경계가 필요해지면 그때 모듈 분리를 검토한다.

<a id="schedule"></a>
### 3. 스케줄 컨텍스트 (`schedule`) — Core Subdomain ★핵심

| 항목 | 내용 |
| --- | --- |
| 책임 | 작업 등록부터 완료까지 전체 생애주기. 발주–배정–수행의 최소 단위를 다룸 |
| 상태 | 구현 중 — Work 애그리게잇·값객체·배정 이력·완료보고·일정 겹침 정책, Application(작업 등록·기본정보 수정·배정·재배정·일정 변경·배정 해제 유즈케이스·출력 포트·오류 코드), 기사 일정 잠금 어댑터(PostgreSQL), 임시 출력 어댑터. 작업 지침은 [schedule 지침](../../src/main/java/com/orbit/schedule/AGENTS.md) |
| 소유 애그리게잇 | **Work**(Root) — 소속 조직(Organization ID 참조), 작업명(사용자 입력 중 유일한 필수 항목), 등록자·담당기사(Membership ID 참조), 시간, 상태<br>├ AssignmentHistory(내부 엔티티) — 배정 시도마다 일정·배정/응답 시각·결과, 최신 이력이 현재 배정<br>├ CompletionReport(내부 엔티티) — 완료보고(사진·메모·실제 결제), Work와 생명주기 완전히 묶임<br>└ WorkSchedule/CustomerInfo/PaymentInfo/Money(VO) — 담당기사·시작시각·예상소요시간 / 고객정보 / 결제정보 / 원 단위 금액 |
| 상태 전이 | 등록(대기함) → 수락대기 → 수락됨 → 작업중 → 완료. 거절·배정 해제는 대기함으로 복귀, 취소는 소프트 삭제인 별도 종료 경로 — 상세는 [작업 상태·배정 정책](#schedule-policies) |
| 읽기 모델 | Timetable/Backlog — **애그리게잇 아님.** Work를 기사×시간 축으로 투영한 조회 결과일 뿐, 자체 쓰기 불변식이 없음 |
| 관계 | `organization` → 참조(ID) (등록자·담당기사가 Work의 조직에 속한 활성 Membership인지, 작업 유형이 같은 조직의 WorkType인지 확인, ACL — 도메인은 ID만 보유하므로 Application이 등록·기본정보 수정·배정·재배정 시 검증. 작업 유형은 새 값이 있을 때만 확인. 일정 변경 때 현재 담당기사를 다시 확인할지는 계약 연결 때 정한다. organization 계약 연결 전에는 소속·작업 유형을 검증하지 못하므로 해당 유즈케이스를 컨트롤러로 노출하지 않는다) · `notification` ← 이벤트 발행(작업 상태 변경) |

> **명명 정정**: 기획안 원문·이전 대화에서는 이 애그리게잇을 "Job"이라 불렀으나, 저장소의 기존 계획 문서(`use-cases.md`)가 이미 `*WorkUseCase` 명명을 쓰고 있어 **애그리게잇명은 "Work"로 통일**한다. 이후 모든 문서·코드에서 Job이라는 이름은 쓰지 않는다.
>
> **모듈명과 애그리게잇명 구분**: BC(모듈) 이름은 `schedule`, 그 안의 유일한 애그리게잇 이름은 `Work`다. 모듈은 작업 등록부터 완료까지 전체 스케줄링 생애주기를 다루는 컨텍스트 단위이고, 애그리게잇은 그 컨텍스트가 소유하는 구체적 도메인 객체다 — 이름이 다른 것은 의도된 설계이며 오기가 아니다.

<a id="schedule-policies"></a>
#### 작업 상태·배정 정책

- 기준일: 2026-09-23 — PR #4~#7 리뷰에서 정책 결정이 필요한 항목을 확정했다.

**진행 상태**는 `REGISTERED`(등록) · `PENDING_ACCEPTANCE`(수락대기) · `ACCEPTED`(수락됨) · `IN_PROGRESS`(작업중) · `COMPLETED`(완료) · `CANCELLED`(취소)다. 대기함은 상태가 아니라 기사·시간이 없는 `REGISTERED` 작업을 보여주는 조회 조건이다.

| 전이 | 출발 상태 | 도착 상태 | 규칙 |
| --- | --- | --- | --- |
| 배정 | 등록 | 수락대기 | 담당기사·시작시간·예상소요시간을 한 번에 지정한다. 같은 기사 일정과 겹치면 경고 후 확인한 요청만 저장한다 |
| 수락 | 수락대기 | 수락됨 | 현재 배정된 기사 본인만 |
| 거절 | 수락대기 | 등록(대기함) | 거절 사유 필수. 거절은 상태가 아니라 배정 이력의 결과로만 남긴다 |
| 재배정 | 수락대기·수락됨 | 수락대기 | 담당기사를 바꾼다. 새 기사에게 다시 수락받는다 |
| 일정 변경 | 수락대기·수락됨 | 수락대기 | 같은 기사에서 시간만 바꾼다. 기사가 수락한 것은 원래 시간이므로 다시 수락받는다. 자기 자신의 기존 일정은 충돌 검사에서 제외한다 |
| 배정 해제 | 수락대기·수락됨 | 등록(대기함) | 작업중·완료는 해제할 수 없다 |
| 시작 | 수락됨 | 작업중 | 담당 기사 본인만. 같은 기사에게 이미 작업중인 작업이 있으면 거부한다 |
| 완료보고 제출 | 작업중 | 완료 | 보고 저장과 완료 전환은 함께 성공·실패한다. 제출 후 보고는 수정할 수 없다 |
| 취소 | 등록·수락대기·수락됨·작업중 | 취소 | 사유 필수. 작업중 취소는 고객 부재·현장 사정 등 중단을 위한 경로다 |

- **기본정보 수정**: 작업명·작업 유형·고객정보(현장 주소 포함)·결제정보(요금)는 등록·수락대기·수락됨·작업중에서 바꿀 수 있고, 완료·취소된 작업은 바꿀 수 없다. 네 항목을 한꺼번에 교체하며 비운 항목은 지운다. 상태 전이가 아니며 기존 배정·수락은 유지한다(일정 변경과 달리 다시 수락받지 않는다). 바뀐 내용을 담당 기사에게 알릴지는 알림 연동 때 정한다.
- **상태를 유지하는 변경**: 수락대기 중의 재배정·일정 변경은 배정 이력만 바뀌고 상태는 그대로이므로 상태 전이가 아니다. 상태 전이 규칙은 자기 자신으로의 전이를 허용하지 않는다.
- **완료보고 입력 상한**: 작업 전·후 사진은 각각 최대 6장, 작업 메모와 사용 부품(자유 텍스트)은 각각 최대 255자다. 사진의 필수 여부·최소 개수·파일 형식·크기는 미결정이다.
- **금액**: 원화에는 소수 단위가 없으므로 작업 금액과 실제 결제 금액은 원 단위 정수 값객체 `Money`로 다루고 음수를 허용하지 않는다. 저장은 정수 컬럼으로 한다.
- **결제 방식**: 등록 시 계획 결제 방식(현장 카드·현장 현금·계좌이체)과 완료보고의 실제 결제 방식(신용카드·현금·계좌이체)은 1:1로 대응한다. 청구서 발행 후불은 제공하지 않는다.
- **취소 불가 상태**: 완료는 완료보고와 이후 정산 근거를 보존하기 위해 취소하지 않는다. 잘못 완료된 작업은 관리자 강제 변경으로 정정한다. 취소된 작업은 다시 바꿀 수 없다.
- **배정 이력**: 배정 시도마다 담당기사·일정·배정 시각(`assignedAt`)·응답 시각(`decidedAt`)·결과를 남긴다. 응답 전에 재배정·일정 변경·배정 해제·취소되면 대기 중이던 이력을 마감하고, 이미 수락·거절로 확정된 이력의 결과는 바꾸지 않는다. 현재 배정은 최신 이력이다. 새 배정 시각은 최신 이력의 마지막 시각(응답 시각, 없으면 배정 시각)보다 앞설 수 없다.
- **관리자 강제 변경**: 허용 경로 없이 상태만 주입하는 방식은 불변식을 깨므로 도메인에 두지 않는다. `CorrectWorkStatusUseCase`를 구현할 때 명시적 전이표와 도착 상태의 필수 데이터 검증을 함께 도입한다.
- **저장 상태 복원**: 복원할 때도 상태와 데이터의 정합성(수락대기 이후 일정 존재, 완료 시 완료보고 존재, 배정 이력의 시간 순서 등)을 검증한다.
- **시각**: 작업 일정과 이력 시각은 UTC `Instant`로 다룬다([트랜잭션과 시간](../conventions/persistence/transactions.md#transactions)). 화면의 날짜·시간 표시는 조직의 현지 시간대로 변환한다.
- **일정 충돌 판정**: 같은 기사의 활성 작업(수락대기·수락됨·작업중)과 반열림 구간 `[시작, 종료)`이 겹치는지 본다. 배정할 수 있는 요청(상태·입력 통과)만 판정하며, 겹치는데 확인하지 않은 요청은 반영하지 않고 겹친 작업을 시작시각 순으로 돌려준다. 확인한 요청은 겹침을 허용해 반영한다. 같은 조직·기사의 일정 변경(배정·재배정·일정 변경)은 겹침 판정 전에 기사 단위로 한 줄로 세워, 동시에 들어온 요청도 앞선 요청의 저장 결과를 보고 판정한다. 기다림이 대기 한도를 넘기면 "잠시 후 다시 시도"로 거부한다(409).

**행위별 권한 (MVP 기본값)** — 역할은 조직 소속(Membership)의 role을 따른다. 직원 유형(예: 상담·팀장)은 분류용 라벨이며 권한에 반영하지 않는다.

| 행위 | 총관리자 | 직원 | 기사 |
| --- | --- | --- | --- |
| 등록·기본정보 수정·배정·재배정·일정 변경·배정 해제·취소 | 가능 | 가능 (등록자 제한 없음) | 불가 |
| 관리자 강제 상태 변경 | 가능 | 불가 | 불가 |
| 수락·거절·시작·완료보고 | 불가 | 불가 | 담당 기사 본인만 |

<a id="notification"></a>
### 4. 알림 컨텍스트 (`notification`) — Generic Subdomain

| 항목 | 내용 |
| --- | --- |
| 책임 | 다른 컨텍스트의 이벤트를 구독해 알림을 생성. 읽음/안읽음 관리 |
| 상태 | 골격 생성 |
| 소유 개념 | **Notification** — 수신자·메시지·읽음여부. 상태가 얕아 별도 VO 없음 |
| 관계 | `organization`, `schedule` ← 이벤트 구독 (Published Language). 양쪽 모듈 모두 알지 못하는 순수 소비자 |

---

## P2 — 다음 확장 (미착수, 코드 없음)

| 컨텍스트 | 서브도메인 | 책임 | 관계 |
| --- | --- | --- | --- |
| **검수** | Supporting | 완료보고 승인/반려 | `schedule` → 참조(완료보고 조회, Customer-Supplier) · `schedule` ← 이벤트(판정 결과) |
| **채팅** | Generic | 카톡 딥링크 대체 검증 후 자체 채팅 도입 | `organization`/`schedule` → 참조(ID) (workId·membershipId로 스레드 스코프만) |
| **기사 가용시간** | — (`schedule` 내부 확장) | 거절률이 높을 때 도입 검토. 배정 후보 필터링용 | `schedule` 내부 애그리게잇으로 추가 (별도 모듈 아님) |

## P3 — 데이터 축적 후 확장 (미착수)

| 컨텍스트 | 서브도메인 | 책임 | 관계 |
| --- | --- | --- | --- |
| **정산** | Supporting | 완료 작업의 금액 집계·정산 | `schedule` → ACL(완료 스냅샷만 참조, 얇은 모델) |
| **리포트·통계** | Generic | 전 컨텍스트 이벤트 기반 순수 읽기 집계 | 전 컨텍스트 ← 이벤트 구독 (쓰기 없음) |
| **재고·문서함** | Generic | 자재·문서 관리 | `schedule` → 참조(ID) (첨부파일 id만) — 통합 자체를 최소화(Separate Ways) |

## P4 — 장기 지향점 (미착수)

| 컨텍스트 | 서브도메인 | 책임 | 관계 |
| --- | --- | --- | --- |
| **고객 셀프예약** | Core-adjacent | 최종고객이 직접 빈 슬롯을 선택해 예약 | `schedule`의 Timetable 읽기모델 → 참조 (Open Host Service, 가용 슬롯 조회) · `schedule` ← 참조(예약=Work 초안 생성, Customer-Supplier) |

---

## 미해결 이슈

| # | 이슈 | 비고 |
| --- | --- | --- |
| BC-001 | 개인 계정·프로필 관리(마이페이지, 탈퇴)를 어느 컨텍스트가 가질지 | `auth`가 가질지, 별도 "계정" 성격 컨텍스트를 새로 둘지 미정. "계정"이라는 이름을 `organization`에서 뺀 것과 직접 연결된 이슈 |
| BC-002 | 참여 요청(MembershipRequest)을 `organization`에서 분리할 시점 | 현재는 승인·소속 생성의 원자성 때문에 `organization`에 둠 — 요청 생명주기가 복잡해지면 분리 검토 |
| BC-004 | 정산 기능의 실제 포함 여부 | 탈퇴 차단 조건에 "미완료 정산"이 등장하지만 정산 업무 흐름 자체는 미확인 (`use-cases.md` §8) |
| BC-005 | 여러 조직에 소속된 기사의 조직 간 일정 충돌 판정 여부 | 일정 충돌은 기사의 Membership(조직별로 따로 존재)으로 판정해 같은 조직 안에서만 본다. 다중 소속이 허용되고 기사 앱이 모든 조직의 작업을 합쳐 보여주므로, 한 사람의 조직 간 겹침을 막을지 제품 결정 필요 |

---

## 다음 단계

1. `organization`/`schedule`/`notification`의 `domain` 패키지에 실제 애그리게잇 구현 (Organization/MembershipRequest/Membership, Work, Notification)
2. 각 모듈 루트에 공개 계약 클래스 배치 (예: `schedule`의 `WorkLookup`처럼 `auth`/`user` 예제와 동일한 패턴)
3. 모듈별 `AGENTS.md` 작성 (도메인 코드가 들어가는 시점에 함께)
4. 이 문서와 [도메인 지도](README.md)를 실제 구현 진행에 맞춰 갱신
